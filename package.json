{
  "name": "perplexity-fusion-search",
  "version": "1.0.0",
  "description": "Production-ready Perplexity AI search agent with intelligent web scouting capabilities",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pplx": "dist/cli.js"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outfile=dist/cli.js --target=node",
    "build:binary": "bun build --compile src/cli.ts --outfile=dist/pplx",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bun tsc --noEmit",
    "lint": "bun run --bun eslint src/**/*.ts",
    "lint:fix": "bun run --bun eslint src/**/*.ts --fix",
    "clean": "rm -rf dist",
    "dev:debug": "bun --inspect src/cli.ts"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "perplexity": "^1.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "bun-types": "latest",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "perplexity",
    "search",
    "ai",
    "cli",
    "tool",
    "typescript",
    "bun",
    "api"
  ],
  "author": "Tool Generator",
  "license": "MIT",
  "files": [
    "dist",
    "src",
    "package.json"
  ]
}
